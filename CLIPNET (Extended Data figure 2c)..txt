#change the file name accordingly.
clipnet attribute \
    -f data/test.fa.gz \
    -o data/test_quantity_shap.npz \
    -m clipnet_models/ \
    -a quantity \
    -v

# -c maybe needed to avoid precision errors.

# use clipnet_transform.ipynb to process to TF-MoDISco, replace the file name with the real name, motif database: JASPAR2024_CORE_vertebrates_non-redundant.meme

